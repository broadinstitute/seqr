# Placeholder for google service account key 

# It is used within the pipeline-runner component when loading data into elasticsearch. It allows hail to directly access files in public google buckets (gs://seqr-reference-data) via 
# the Cloud Storage connector without having to download them first.

# To create this key file, see instructions @ https://cloud.google.com/dataproc/docs/concepts/connectors/install-storage-connector
