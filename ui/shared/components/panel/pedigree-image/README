Pedigree image that can be clicked on to show a modal dialog with a larger version of the image.

To use this panel:

- add pedigree zoom modal state to your redux state tree. In your root reducer, do:

    import { pedigreeImageZoomModalState } from 'shared/components/panel/pedigree-image-zoom-modal/state'

    const rootReducer = combineReducers({
        otherState: otherReducer,
        otherState2: otherReducer2,

        ...pedigreeImageZoomModalState,
    })


- add PedigreeImageZoomModal to your UI so that it can be shown

- add PedigreeImage(s) to your UI

